// networking:
// Display local IP --> type it into join dialog on non-host laptop
// --> it joins host & host now knows it has +1 laptops connected to it (yay)

// Things to send for clients to render
// # On connection:
// Physics tick rate
// Number of clients (sent to every client whenever a client connects)

// Things to send at the start of every hit:
// Every ball's position & velocity

// Things to send after the end of a hit:
// Every ball's position
// Player's turn